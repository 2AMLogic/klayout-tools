{
  "name": "loom-workspace",
  "description": "Loom-managed workspace with AI development orchestration",
  "private": true,
  "scripts": {
    "worktree:return": "./.loom/scripts/worktree-return.sh",
    "check:ci": "npm run lint && npm run test",
    "check:all": "npm run check:ci",
    "test": "KLT_SKIP_NGSPICE_TESTS=1 KLT_SKIP_OPENROAD_TESTS=1 uv run --extra dev pytest",
    "lint": "uv run --extra dev ruff format --check . && uv run --extra dev ruff check .",
    "format": "uv run --extra dev ruff format ."
  }
}
