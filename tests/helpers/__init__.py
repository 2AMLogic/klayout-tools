"""Shared test-only helper modules (not part of the `klayout_tools` package).

Nothing under `tests/helpers/` is importable from the installed `klt`
distribution -- it exists purely to support the test suite (mirrors
`tests/corpus/generate_golden.py`'s "test-scoped, not shipped" status).
"""
