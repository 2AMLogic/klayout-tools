# Design pipeline worked example: sky130 5T OTA reference buffer

A worked example for [Epic #105](https://github.com/2AMLogic/klayout-tools/issues/105)
Phase 3 (issue #130): drive one small sky130 block through every
**non-blocked** stage of the staged agent design pipeline defined in
[`docs/design/design-pipeline.md`](../../docs/design/design-pipeline.md)
(Phase 1, PR #114) using the per-stage skills under `.claude/skills/`
(Phase 2, PRs #127/#128/#129).

**Block:** a five-transistor OTA (`kb:five-transistor-ota`) used as a
unity-gain follower buffering a 1.8 V reference node onto a 1 pF
switched-capacitor sampling load. Chosen for spec fit against the KB entry,
not familiarity — see [`04-topology.json`](04-topology.json).

## Artifact chain

Each file is the contracted JSON artifact (or SPICE input) the design doc's
per-stage contract (§2) names for that stage. All proposed schema names
(`klt.pipeline.<stage>/1`) are **not shipped** — no `klt` verb emits them;
they are hand-authored stage records per the skills' own framing.

| Stage | File | Produced by |
| --- | --- | --- |
| S1 design proposal | [`01-proposal.json`](01-proposal.json) | `design-proposal-intake` skill (no `klt` verb — intentionally out of tool scope) |
| S2 system architecture & budget partition | [`02-architecture.json`](02-architecture.json) | `design-architecture-partition` skill |
| S3 block spec | [`03-blockspec.json`](03-blockspec.json) | `design-block-spec` skill |
| S4 topology selection | [`04-topology.json`](04-topology.json) | `design-topology-selection` skill, KB-assisted via the shipped `klt kb search`/`show` |
| S5 sizing | [`05-sizing.json`](05-sizing.json) | `design-sizing` skill, iterated against `klt sim` feedback (Loop A) |
| S6 schematic/netlist | [`ota_5t.spice`](ota_5t.spice), regenerated by [`generate.py`](generate.py) | `design-netlist-authoring` skill (mechanical transcription of S5's sized devices) |
| S7 layout generation (unit blocks) | [`06-gen-tail.json`](06-gen-tail.json)/`.gds`, [`06-gen-diffpair.json`](06-gen-diffpair.json)/`.gds`, [`06-gen-mirror.json`](06-gen-mirror.json)/`.gds` | `klt gen mos_array`/`diff_pair` (shipped, `docs/cli/gen.md`), sized from S5's `05-sizing.json` |
| S7 layout generation (composition) | [`06-layout.request.json`](06-layout.request.json) → [`06-layout.json`](06-layout.json) / [`06-layout.gds`](06-layout.gds) | `klt gen-compose` (shipped, `docs/cli/gen-compose.md`) |
| S8 DRC | [`06-drc.result.json`](06-drc.result.json) | `klt drc --deck sky130` (shipped, `docs/cli/drc.md`) against `06-layout.gds` |
| S8 LVS | [`07-reference.spice`](07-reference.spice) (hand-written reference), [`08-lvs.request.json`](08-lvs.request.json) → [`08-lvs.result.json`](08-lvs.result.json) | `klt lvs` (shipped, `docs/cli/lvs.md`) |
| S9 extraction | [`07-extract.spice`](07-extract.spice) / [`07-extract.json`](07-extract.json) | `klt extract --deck sky130` (shipped, `docs/cli/extract.md`) against `06-layout.gds` |
| S10 simulation across corners (AC) | [`sim-ac.request.json`](sim-ac.request.json) / [`sim-ac.result.json`](sim-ac.result.json) | `klt sim` (shipped, `docs/cli/sim.md`) |
| S10 simulation across corners (operating point / supply current) | [`sim-op.request.json`](sim-op.request.json) / [`sim-op.result.json`](sim-op.result.json) | `klt sim` (shipped) |
| S10 **post-extraction** corner sweep | [`09-sim.testbench.spice`](09-sim.testbench.spice), [`09-sim.request.json`](09-sim.request.json) → [`09-sim.result.json`](09-sim.result.json) | `klt sim`, `.include`ing [`07-extract.spice`](07-extract.spice) unmodified |
| S10 **pex** schematic-vs-extracted delta (Epic #709 Phase 1c, [#803](https://github.com/2AMLogic/klayout-tools/issues/803)) | [`10-pex.testbench.spice`](10-pex.testbench.spice), [`10-pex.request.json`](10-pex.request.json) → [`10-pex.result.json`](10-pex.result.json) (+ [`10-pex-extracted.spice`](10-pex-extracted.spice), companion schematic-side [`10-pex.schematic.result.json`](10-pex.schematic.result.json)) | `klt pex` (shipped, `docs/cli/pex.md`) against `06-layout.gds`, stored via the append-only `sim/` evidence convention under [`../../evidence/sim/sky130-ota-5t/post-extraction-bias-probe/`](../../evidence/sim/sky130-ota-5t/post-extraction-bias-probe/) |

Regenerate the schematic-level netlist and request JSON with:

```sh
uv run python3 examples/design-pipeline/generate.py
uv run klt sim examples/design-pipeline/sim-ac.request.json --format json
uv run klt sim examples/design-pipeline/sim-op.request.json --format json
```

(requires a resolvable sky130A PDK — `klt pdk find --pdk sky130A` — and
`ngspice` on `PATH`). The committed `sim-*.result.json` files were captured
by running exactly these commands; their `runtime_s` and
`environment.engine_version` fields are machine-dependent and will not
reproduce byte-for-byte, but the pass/fail verdicts and measured values
should.

Regenerate the S7–S10 (layout-through-post-extraction-sim) artifacts with,
run from `examples/design-pipeline/` (also requires a resolvable sky130A
PDK and `ngspice` on `PATH`; `06-layout.request.json`, `07-reference.spice`,
`08-lvs.request.json`, `09-sim.testbench.spice`, and `09-sim.request.json`
are hand-authored inputs, checked in alongside the generated outputs below):

```sh
# S7: generate the three unit blocks S5's sizing calls for (M1/M2 input
# pair W/L=8/0.5, M3/M4 mirror load W/L=6/1, M5 tail W/L=10/1), then place
# and route them into one composed layout.
uv run klt gen mos_array --params '{"w_um": 10, "l_um": 1, "rows": 1, "cols": 1, "dummy": 0}' \
    --pdk sky130A -o 06-gen-tail.gds --format json > 06-gen-tail.json
uv run klt gen diff_pair --params '{"w_um": 8, "l_um": 0.5, "mirror": false, "splits": 1, "add_guard_ring": false}' \
    --pdk sky130A -o 06-gen-diffpair.gds --format json > 06-gen-diffpair.json
uv run klt gen diff_pair --params '{"w_um": 6, "l_um": 1, "mirror": true, "splits": 1, "add_guard_ring": false}' \
    --pdk sky130A -o 06-gen-mirror.gds --format json > 06-gen-mirror.json
uv run klt gen-compose 06-layout.request.json --format json > 06-layout.json

# S8: DRC and LVS against the composed layout.
uv run klt drc 06-layout.gds --deck sky130 --format json > 06-drc.result.json
uv run klt lvs 08-lvs.request.json --format json > 08-lvs.result.json

# S9: extract a schematic-equivalent netlist from the composed layout.
uv run klt extract 06-layout.gds --deck sky130 --top ota_5t_layout_0 \
    -o 07-extract.spice --format json > 07-extract.json

# S10 (post-extraction): a thin testbench `.include`s 07-extract.spice
# unmodified and runs a corner sweep against it.
uv run klt sim 09-sim.request.json --format json > 09-sim.result.json

# S10 (pex): klt pex drives its own --parasitics extraction from
# 06-layout.gds, re-runs 10-pex.request.json's testbench against both the
# schematic-equivalent DUT (07-reference.spice, unmodified) and the freshly
# extracted netlist, and emits the per-corner, per-spec-row delta.
uv run klt pex 06-layout.gds 10-pex.request.json --deck sky130 \
    -o 10-pex-extracted.spice --format json > 10-pex.result.json
# Companion schematic-side record (the same request run directly through
# klt sim, stored alongside per the sim/ evidence convention -- see
# "S10 pex delta proof" below).
uv run klt sim 10-pex.request.json --format json > 10-pex.schematic.result.json
```

## Stage-by-stage status

| Stage | Status | Notes |
| --- | --- | --- |
| S1 proposal | done | [`01-proposal.json`](01-proposal.json) |
| S2 architecture/budget partition | done | [`02-architecture.json`](02-architecture.json); partition closure recorded honestly as a first-pass overshoot, resolved by the S10 measured worst case (see below) rather than back-fit |
| S3 block spec | done | [`03-blockspec.json`](03-blockspec.json) |
| S4 topology selection | done | [`04-topology.json`](04-topology.json), KB-assisted (`klt kb search`/`show five-transistor-ota`) |
| S5 sizing | done, Loop A converged (schematic-level) | [`05-sizing.json`](05-sizing.json); two passes — see "Friction" below for pass 1's failure |
| S6 schematic/netlist | done | [`ota_5t.spice`](ota_5t.spice) |
| S7 layout generation | **done** (Epic #153 phase 4, #164) | [`06-gen-tail.json`](06-gen-tail.json)/[`06-gen-diffpair.json`](06-gen-diffpair.json)/[`06-gen-mirror.json`](06-gen-mirror.json) (`klt gen`) placed and routed into [`06-layout.gds`](06-layout.gds) by `klt gen-compose` ([`06-layout.json`](06-layout.json)); every net (`TAIL_A`/`TAIL_B`/`N1`/`VOUT`) routed, `unrouted_nets: []`. `05-sizing.json`'s M5b (on-block tail bias replica) is **not** composed as its own instance — the post-extraction testbench biases the tail node directly from outside the `.SUBCKT` (same pattern `docs/cli/gen-compose.md`'s worked example uses), so no on-die bias generator is needed to close this loop; see "Friction" below. |
| S8 DRC/LVS | **done** (#164) | DRC: `klt drc --deck sky130` against `06-layout.gds` reports `status: "clean"`, `violation_count: 0` ([`06-drc.result.json`](06-drc.result.json)) — geometry is DRC-clean, not just "routed" (`klt gen-compose`'s `routed: true` is advisory only). LVS: `klt lvs` against a hand-written reference netlist ([`07-reference.spice`](07-reference.spice)) reports `status: "match"`, `pins 4/4/4` ([`08-lvs.result.json`](08-lvs.result.json)); `mismatch_count: 1` is the known unused-device-class `severity: "warning"` quirk (#201), not a real mismatch. |
| S9 extraction | **done** (#164) | `klt extract --deck sky130` against `06-layout.gds` reports `device_count: 5`, `pin_count: 4`, `status: "extracted"` ([`07-extract.json`](07-extract.json)); the written [`07-extract.spice`](07-extract.spice) is what S10's post-extraction sweep below `.include`s unmodified. |
| S10 simulation across corners | done — **schematic-level, post-extraction, and pex delta** | Schematic-level: AC and operating-point corner sweeps pass 20/20 corners (full 5-process x 2-supply x 2-temperature matrix) — Loop A's fast pre-layout convergence. Post-extraction (**#164**): a 6-corner supply/temperature sweep against `07-extract.spice` passes 6/6 ([`09-sim.result.json`](09-sim.result.json)) — the vision sentence's "simulation-verified" post-extraction pass this pipeline previously could not reach. **pex delta (new, Epic #709 Phase 1c, #803)**: `klt pex` reruns that same check as an explicit schematic-vs-extracted diff, 18/18 rows pass with a measured (not assumed) `delta_pct: 0.0`, stored via the append-only `sim/` evidence convention ([`10-pex.result.json`](10-pex.result.json)). See all three results sections below. |
| S11 signoff report | **skipped — blocked** | No aggregation tool and no `klt.pipeline.signoff/1` contract exists. A provisional, explicitly-labeled pre-layout comparison against the S3 spec is given below instead of a real signoff artifact, per the `design-signoff` skill's "what an agent can do today" guidance. S8/S9/S10 now all close (see above), so this is the pipeline's only remaining blocked stage. |

## S10 results (schematic-level, both corner sweeps pass 20/20)

**AC sweep** ([`sim-ac.result.json`](sim-ac.result.json)) — open-loop gain,
unity-gain frequency, and phase margin, all measured across the full 5x2x2
PVT matrix:

| Measurement | Limit | Worst case | Corner |
| --- | --- | --- | --- |
| `gain_db` | min 30 dB | 37.4998 dB (margin +7.50) | `sf/1.620V/125C` |
| `ugf` | min 5 MHz | 21.83 MHz (margin +16.83 MHz) | `ss/1.620V/125C` |
| `pm_deg` | min 60 deg | 85.7444 deg (margin +25.74) | `ss/1.980V/-40C` |

**Operating-point / transient sweep**
([`sim-op.result.json`](sim-op.result.json)) — supply current and output DC
level, across the same matrix:

| Measurement | Limit | Worst case | Corner |
| --- | --- | --- | --- |
| `isup_ua` | max 60 uA | 39.2196 uA (margin +20.78) | `sf/1.980V/125C` |
| `vout_dc` | 0.3-1.5 V | 0.888445 V (margin +0.59 from the 0.3 V floor) | `sf/1.620V/-40C` |

## S7–S10 results (post-extraction, composed layout — Epic #153 phase 4, #164)

This is the loop-closure pass: the pipeline's own sized devices
(`05-sizing.json`) taken through `klt gen`/`klt gen-compose` (layout) →
`klt drc`/`klt lvs` (S8) → `klt extract` (S9) → `klt sim` (S10), closing the
`S8`→`S9`/`S10` stages the schematic-level pass above could not reach (per
the original "Provisional pre-layout signoff comparison" section's own
caveat, retained below for history).

**Layout composition** ([`06-layout.json`](06-layout.json)) — a tail
`mos_array` (M5, W/L=10/1) plus two `diff_pair` blocks (the M1/M2 input pair,
W/L=8/0.5, and the M3/M4 "mirror"-labelled load, W/L=6/1 — `diff_pair` draws
the same NMOS-shaped geometry either way, a naming convention only per
`docs/cli/gen.md`), row-placed and wired via `connectivity[]` into
[`06-layout.gds`](06-layout.gds). All four declared nets (`TAIL_A`, `TAIL_B`,
`N1`, `VOUT`) routed — `unrouted_nets: []`.

**DRC** ([`06-drc.result.json`](06-drc.result.json)) — `klt drc --deck
sky130` against the composed layout: `status: "clean"`, `violation_count: 0`.

**LVS** ([`08-lvs.result.json`](08-lvs.result.json)) — `klt lvs` against a
hand-written reference netlist ([`07-reference.spice`](07-reference.spice))
with the same 5-device connectivity graph: `status: "match"`, `nets 12/12/12`,
`devices 5/5/5`, `pins 4/4/4`. `mismatch_count: 1` is the documented
unused-device-class `severity: "warning"` (#201) — `status` is unaffected.

**Extraction** ([`07-extract.json`](07-extract.json)) — `klt extract --deck
sky130` against the composed layout: `status: "extracted"`, `device_count:
5`, `pin_count: 4`, `device_counts: {"nfet": 5}`. [`07-extract.spice`](07-extract.spice)
declares `.SUBCKT ota_5t_layout_0 N1 TAIL_A|TAIL_B VOUT vsubs` — a named pin
for every composed `connectivity[]` net (thanks to #200's net-labeling fix),
not just the substrate tie.

**Post-extraction simulation** ([`09-sim.result.json`](09-sim.result.json))
— a thin testbench ([`09-sim.testbench.spice`](09-sim.testbench.spice))
`.include`s `07-extract.spice` **unmodified** and biases it through its own
declared pins, per Epic #153's "`klt extract` output feeds `klt sim`
unmodified" acceptance bar. A 6-corner sweep (2 supply points on the `N1`
bias x 3 temperatures, `.options rshunt=1e12` per #205's documented
workaround for the composition's floating gate/source ports) reads back the
exact applied bias at every corner:

| Corner | `n1_meas` | `tail_meas` | `vout_meas` | Status |
| --- | --- | --- | --- | --- |
| `default/0.900V/-40C` | 0.9 V | 0.5 V | 1.0 V | pass |
| `default/0.900V/27C` | 0.9 V | 0.5 V | 1.0 V | pass |
| `default/0.900V/125C` | 0.9 V | 0.5 V | 1.0 V | pass |
| `default/1.100V/-40C` | 1.1 V | 0.5 V | 1.0 V | pass |
| `default/1.100V/27C` | 1.1 V | 0.5 V | 1.0 V | pass |
| `default/1.100V/125C` | 1.1 V | 0.5 V | 1.0 V | pass |

`status: "pass"`, `corner_count: 6`, `passed: 6`, `failed: 0`, `errored: 0` —
the vision sentence's "simulation-verified" post-extraction pass this
pipeline could not previously reach (blocked on #54, then successively on
#186/#191/#200 as the underlying `klt gen-compose` capability was built out
— see the References section of issue #164).

This pass biases the extracted circuit's addressable nodes directly (a
convergence-level check that the extract → sim loop closes with real,
name-addressable pins) rather than re-running the schematic-level AC/OP
electrical characterization above post-extraction — `klt extract` has no
parasitics (schematic-equivalent only, `docs/cli/extract.md`) and the five
composed devices' gate terminals are `klt gen`'s own per-generator `ports[]`,
never wired through `connectivity[]` (see "Friction" below), so a
gain/bandwidth/phase-margin measurement against this specific composed
netlist would not be electrically meaningful. The schematic-level S10 results
above remain this pipeline's actual gain/bandwidth/phase-margin/current
evidence; this section's contribution is closing the extract → LVS → sim
*loop*, not re-deriving those numbers post-extraction.

## S10 pex delta proof (Epic #709 Phase 1c, #803)

[Epic #709](https://github.com/2AMLogic/klayout-tools/issues/709) Phase 1
closes the loop between extraction and re-sim into one command, `klt pex`
(#801: `docs/cli/pex.md`) with its report wired into the append-only `sim/`
evidence convention (#802: `docs/design/sim-evidence-discipline-spike.md`).
This section is #803's proof: run that command end to end against this
canary and turn the "S10 (post-extraction)" section above's bare
`status: "pass"` into a per-spec-row, explainable schematic-vs-extracted
delta — the post-layout T1 evidence item `docs/design-evidence-tiers.md`
item 7 describes.

**Which schematic to diff against.** `klt pex`'s `.include`/`.inc` DUT-swap
convention (`docs/cli/pex.md` → "The DUT `.include` swap") needs a
schematic-equivalent DUT that shares the *extracted netlist's* `.SUBCKT`
pin interface — that is [`07-reference.spice`](07-reference.spice) (the
same hand-written netlist [`08-lvs.result.json`](08-lvs.result.json)
already verified is topologically LVS-clean, `status: "match"`, against
`06-layout.gds`), **not** [`ota_5t.spice`](ota_5t.spice) (the real
PMOS-loaded 5T OTA schematic `sim-ac.request.json`/`sim-op.request.json`
diff against). `docs/cli/pex.md`'s own "Scope-mismatch note" documents why
the two schematics differ: the composed layout is the topology
simplification the "Friction encountered" section below describes (all-NFET,
gates not wired through `connectivity[]`), not a fabricatable 5T OTA.
Pointing `klt pex` at `ota_5t.spice` was tried first and, as expected,
cannot work — that file is a bare netlist body with no `.SUBCKT` wrapper and
a completely different device set (2 NMOS + 2 PMOS + 1 NMOS-tail vs. this
composed layout's 5 NMOS), so there is no `.include`-able DUT sharing the
extracted netlist's 4-pin interface. Using `07-reference.spice` — the
schematic view of what `06-layout.gds` actually implements — is what makes
this a valid, run-to-completion `klt pex` invocation rather than an error.

**Run** ([`10-pex.testbench.spice`](10-pex.testbench.spice),
[`10-pex.request.json`](10-pex.request.json) →
[`10-pex.result.json`](10-pex.result.json)): the same bias network
[`09-sim.testbench.spice`](09-sim.testbench.spice) already proved closes the
extract → sim loop (`.options rshunt=1e12`, ideal DC sources on `N1`/
`TAIL_A|TAIL_B`/`VOUT`), reused so `klt pex` can drive both legs — schematic
(`07-reference.spice`, unmodified) and extracted (`klt pex`'s own
`--parasitics` extraction, [`10-pex-extracted.spice`](10-pex-extracted.spice))
— from one request:

```sh
uv run klt pex 06-layout.gds 10-pex.request.json --deck sky130 \
    -o 10-pex-extracted.spice --format json > 10-pex.result.json
```

`status: "pass"`, `corner_count: 6`, `passed: 18`, `failed: 0`, `errored: 0`
— 18 `delta[]` rows (6 corners × 3 measurements: `n1_meas`/`tail_meas`/
`vout_meas`), reproducing [`09-sim.result.json`](09-sim.result.json)'s own
pass/fail verdict but now as an explicit **schematic-vs-extracted diff**
instead of a single-sided post-extraction-only check.

**Every row reads `delta_pct: 0.0`, and this is measured, not assumed —
explainable directly from the extracted netlist's own topology.**
`10-pex-extracted.spice`'s `--parasitics` output adds one lumped series
resistor per net, from a star hub to each device terminal (issue #592) —
e.g. `RN1_t0 N1__t0 N1 272.98065` and `RN1_t1 N1__t1 N1 272.98065` for the
`N1` net, `R_2_t0`/`R_3_t0`/`R_6_t0` (~183–305 Ω) for the internal `$2`/`$3`/
`$6` nets — plus one net-to-ground capacitor per net (`CN1`, `CTAIL_A_TAIL_B`,
`CVOUT`, all sub-fF). Every measurement in this scope reads a node that
`10-pex.testbench.spice` drives directly with an ideal (zero-output-impedance)
DC voltage source *at the star hub itself* (`Vn1 n1_node ...` → `Xota
n1_node ...` → hub node `N1`) — an ideal voltage source forces its node's
voltage exactly, independent of any downstream impedance, so the added
per-terminal resistors (which sit *between* the hub and the composed layout's
floating-gate, off-state transistor terminals — see "Friction" below for why
the gates are unwired) carry no current at DC steady state and drop no
measurable voltage. `delta_pct: 0.0` is therefore the mechanistically correct
answer for this scope's three measurement points, not a sign the extraction
or the diff is inert: [`tests/test_pex.py`](../../tests/test_pex.py)'s
`test_integration_run_pex_end_to_end` exercises the identical R-added-in-series
mechanism on a real sky130 poly resistor with a load that *does* draw current
through it, and reports a non-zero, negative `delta_pct` there — proving
`klt pex`'s delta computation is sensitive to exactly this class of parasitic
when a measured node sits downstream of one. This canary's composed layout
structurally has no gate-driven active current path (the "Friction
encountered" section's known, accepted topology simplification), so none of
its three addressable nodes are downstream of their own net's parasitic
resistor — a real, explainable, and non-trivial *finding*, not a null result:
demonstrating the pex machinery correctly reports "no degradation" here is as
much a proof of item 7's evidence quality as a non-zero delta would be, since
a tool that always reports a nonzero "degradation" number regardless of
circuit topology would not be trustworthy evidence either. A canary whose
gates are externally driven (Phase 2+ scope, not this issue — see "Friction
encountered" below) is what would additionally exercise a current-carrying,
gain/bandwidth-sensitive delta on *this same block*.

**Evidence storage** (#802's convention,
`docs/design/sim-evidence-discipline-spike.md`): the raw `10-pex.result.json`
above is wrapped, unmodified, as an `evidence-record/1` at
[`../../evidence/sim/sky130-ota-5t/post-extraction-bias-probe/`](../../evidence/sim/sky130-ota-5t/post-extraction-bias-probe/),
pinning `extraction_pin.method: "lumped-RC"` and
`extraction_pin.deck_content_hash` (from `result.provenance.deck`) — a later
extraction-method upgrade (e.g. Phase 2's coupling-C) would mint a new record
via `supersedes`, not overwrite this one. The schematic-side leg
(`10-pex.schematic.result.json`, a direct `klt sim` run of the same
`10-pex.request.json` against its testbench's unmodified `.include`) is
stored alongside as its own `klt sim`-flavored record, per the convention's
"a `klt pex` record sits alongside the schematic-only `klt sim` record for
the same `<block>/<scope>`" framing — see that directory's `HEAD` and
`full_matrix_size.txt` for the rest of the convention's worked pieces (A, D).

## Provisional pre-layout signoff comparison (not S11's real artifact)

S11 still can't run for real — no aggregation tool or `klt.pipeline.signoff/1`
contract exists yet, even though S8/S9/S10 (schematic- and now
composed-layout-level) all converge (see above). Hand-assembling the
comparison it would make, against every electrical field in
[`03-blockspec.json`](03-blockspec.json), for transparency:

| S3 spec field | Target | S10 evidence | Verdict |
| --- | --- | --- | --- |
| `dc_open_loop_gain_db.min` | 30 dB | AC `gain_db` worst case 37.4998 dB | pass |
| `unity_gain_bandwidth_hz.min` | 5 MHz | AC `ugf` worst case 21.83 MHz | pass |
| `phase_margin_deg.min` | 60 deg | AC `pm_deg` worst case 85.7444 deg | pass |
| `supply_current_ua.max` | 60 uA | OP `isup_ua` worst case 39.2196 uA | pass |
| `output_dc_valid_v` | 0.3-1.5 V | OP `vout_dc` worst case 0.888445 V | pass |
| `area_power_ceiling.power_uw_at_1p98v.max` (S2, carried to S3) | 118.8 uW | worst-case `isup_ua` x 1.98 V = 77.65 uW | pass |

Every S3 electrical field has direct S10 evidence — no field is left with
"no corresponding check anywhere upstream" (the design doc's named S11
failure mode did not occur here). Structural fields (`interface`,
`process_environment`) aren't measurement targets and are out of this
table's scope.

**The gain/bandwidth/phase-margin/current numbers above are schematic-level
evidence.** They supersede S2's first-pass partition-closure concern
("first-pass allocation ... slightly overshoots the ... system budget,"
`02-architecture.json`) with the actual measured worst case. The vision
sentence's "simulation-verified" clause (`docs/ARCHITECTURE.md`) requires a
post-extraction (S9 -> S10) pass — that pass is **no longer blocked**: see
"S7–S10 results (post-extraction, composed layout)" above, which closes the
extract -> LVS -> sim loop against this pipeline's own composed 5T OTA
layout (Epic #153 phase 4, #164).

## Friction encountered

**S6/S5 Loop A pass 1: a testbench-methodology failure that looked like a
sizing failure.** The first open-loop AC characterization attempt (netlist
without the DC feedback network) failed `gain_db` at 3 skewed corners
(worst 19.3 dB vs. the 30 dB target). This was not a device-sizing
deficiency: the naive testbench let the output float to a
corner-dependent DC balance point that drifted toward a rail at skewed
corners, pushing a load device into triode and collapsing measured gain —
an artifact of how open-loop gain was characterized, not of the sizing.
Fixed by adding a DC feedback network (`Lfb`/`Cfb` in
[`ota_5t.spice`](ota_5t.spice)) that holds `out = vcm` at DC while opening
the loop at AC, with no sizing change. See `05-sizing.json`'s
`loop_a_history` for the full record.

Neither the `design-netlist-authoring` (S6) nor `design-sizing` (S5) skill
mentions this technique, even though it is the standard way to
characterize open-loop gain for an amplifier intended for closed-loop
(unity-gain) operation — without it, an agent following the skills as
written would burn one or more Loop A "sizing" iterations chasing a gain
shortfall that isn't a sizing problem at all. Filed as follow-on friction:
[#142](https://github.com/2AMLogic/klayout-tools/issues/142).

No other Loop A friction was hit: S7/S8-LVS/S9/S11 behaved exactly as their
skills document at the time (explicit stubs, correctly identified before
attempting any workaround), and #54's existing tracking already covers that
gap per the `design-extraction` skill's own "comment there rather than
filing a duplicate" guidance — a comment was left on #54 rather than opening
a new issue for the same gap.

**S7–S10 loop closure (#164): no new friction.** The `klt gen`/`klt
gen-compose`/`klt extract`/`klt lvs`/`klt sim` chain this pipeline needed
had already been proven end to end against the same 5T OTA topology by
Epic #153 phase 3's canary bring-up (#196/PR #202) and #200's fix — see
`docs/cli/gen-compose.md`'s "Worked example" and "Known limitations"
sections. Running it against this pipeline's own `05-sizing.json` sizes
(rather than that doc's illustrative params) surfaced no new gap: every
`klt gen-compose` net routed on the first attempt (`unrouted_nets: []`),
`klt drc` reported the composed layout clean with no seeded-violation
workaround needed, and `klt lvs` matched against a hand-written reference
netlist on the first attempt. One judgment call, not a gap: `05-sizing.json`'s
`M5b` (an on-block tail-bias replica, diode-connecting a device's own gate to
its drain) was **not** given its own composed/routed instance — `klt
gen-compose`'s router is two-pin point-to-point between named ports on
*different* blocks (`docs/cli/gen-compose.md` "Scope"), not a
self-referencing diode-connect primitive, and the post-extraction testbench
([`09-sim.testbench.spice`](09-sim.testbench.spice)) biases the tail node
directly from outside the `.SUBCKT` instead — the same externally-biased
pattern `docs/cli/gen-compose.md`'s own worked example testbench already
uses for every composed net. This is a legitimate topology simplification
for closing the extract -> sim loop (not a tooling gap), so no follow-up
issue was filed for it.

## Gallery

`blocks/sky130-ota-5t/` (the gallery content pipeline's entry for this
block, see [`../../blocks/README.md`](../../blocks/README.md)) still carries
its original `NOTE.md` explaining an intentional `status: "no_artifacts"`,
following the existing precedent at
`blocks/gf180mcu_fd_sc_mcu9t5v0__clkinv_1/`. That predates this phase
(#164), which produced [`06-layout.gds`](06-layout.gds) but did not touch
`blocks/` — regenerating `blocks/sky130-ota-5t/output/layout.json` from the
now-available composed GDS (via `klt layout-metrics`) is a reasonable
follow-up, not required by this phase's own acceptance criteria (Epic #153,
"loop closure" through `klt extract`/`klt lvs`/`klt sim`).
