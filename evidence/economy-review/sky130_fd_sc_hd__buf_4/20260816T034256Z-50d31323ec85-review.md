# Economy review: sky130_fd_sc_hd__buf_4 (digital standard cell, sky130)

Reviewed: 2026-08-16 · GDS:
`blocks/sky130_fd_sc_hd__buf_4/output/sky130_fd_sc_hd__buf_4.gds`
(sha256:50d31323ec85291e1631f826576c7add7482994f711756a5f4c4c90eb81153f0) ·
Top cell: `sky130_fd_sc_hd__buf_4`

## Renders

- Overview: `20260816T034256Z-50d31323ec85-images/overview.png` (all
  non-empty layers, whole cell)
- No quadrant crops taken: the cell's own bbox (3.14 x 3.2 um) is already a
  single-glance object at the overview's resolution — a quadrant split adds
  no information a foundry standard cell of this size doesn't already show
  in full.

## Metrics (economy_metrics.py, placeholder pending klt economy #1012)

- bbox: 3.14 x 3.2 um, aspect ratio 0.98 (near-square, as expected for a
  single-row standard cell close to its own row height)
- utilization: **0.9397** (block-kind expectation for digital standard-cell
  rows: >=0.85 typical, >=0.70 floor — comfortably above the typical band)
- dead margins (um, 8x6 grid): left=0.0 right=0.0 bottom=0.0 top=0.0
- empty regions: 0

## Judgment

This is a foundry-authored sky130 standard cell (`sky130_fd_sc_hd__buf_4`),
tight by construction — placed devices, diffusion, poly, and local
interconnect fill essentially the entire drawn bbox up to legal spacing
rules, with no floorplan slack to trim (a standard cell's footprint *is*
its row-height/pitch legal minimum, not a placement choice). The metrics
confirm this directly: utilization 0.94 is comfortably inside the digital
rubric's typical band, and the whitespace grid found zero grid cells below
the 5%-covered threshold and zero measurable dead margin on any edge —
every 8x6 grid cell reads as substantially covered. Visually, the overview
render shows diffusion/poly/local-interconnect geometry reaching all four
cell-boundary edges (abutting the neighboring cell's geometry at the row
boundary, as intended for standard-cell abutment), consistent with the
numeric finding.

There is no isolation/matching structure to account for here (this rubric
axis is specific to analog blocks) — the digital expectation is simply
"is it packed," and it is.

## Verdict

**pass**

No revision targets. This block is cited here specifically as the
known-tight reference point for `economy-review`'s rubric calibration
(issue #1013 acceptance criteria) — contrast against
`evidence/economy-review/sky130-bandgap/`'s `revise` verdict on the same
metrics (utilization 0.38, ~41 um dead margins, 2 empty regions) confirms
the skill's verdicts move in the correct direction between a known-tight
and a known-loose real layout.
